entity Organization {
    id Long,
    externalId String,
    name String required,
    description String,
    organizationType String,
    organizationGroup String
}

entity ParentOrganization {
    id Long,
    name String required
}

relationship OneToOne {
    Organization{parentOrganization} to ParentOrganization
}

